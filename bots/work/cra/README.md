# CRA library layout (canonical)

**Canonical paths = lowercase** (Work filing tree):
- `active/` — cri-formation, cri-bylaws, cri-compliance (incl. texas-pir/)
- `historical/` — drafts + reference-samples/ (AC Capital; NOT CRI)
- `notes/` — batch notes

Uppercase `ACTIVE/`, `HISTORICAL/`, `REFERENCE/` are empty placeholders with NOTES pointing here — do not file new docs there.

Root locked masters stay on Work’s computer under `/workspace/cra/` until Work routes a move; no rewrite without Jeff exact yes.

Hard rules: never file CRI taxes; Comptroller # ≠ federal EIN; EIN digits are not stored in this public hub (prefer Letter 147C/CP575 photo when available); no post.

Updated 2026-09-11 (merge: adopt lowercase).

## Cloud home (2026-09-11)
Work uploaded the CRA library to Jeff’s **iCloud Drive → CRA/** (`active`, `historical`, `notes`, `amendment`, `site`, `README`).
That is the **canonical cloud home** for CRA files. Work’s computer `/workspace/cra/` remains the working copy (this hub holds sanitized notes only).
No rewrite of locked amendment text.

